import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div>
            <h1>Basic Information of All The Countries.</h1>
        </div>
    );
};

export default Header;